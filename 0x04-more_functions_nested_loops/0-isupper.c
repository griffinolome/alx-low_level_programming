#include "main.h"
/**
 * _isupper - runs the code
 * @c: parameter in the code
 * Return: always at end of code
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
